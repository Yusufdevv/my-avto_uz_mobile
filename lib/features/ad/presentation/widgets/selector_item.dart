import 'package:flutter/material.dart';

class SelectorItem extends StatelessWidget {
  const SelectorItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
