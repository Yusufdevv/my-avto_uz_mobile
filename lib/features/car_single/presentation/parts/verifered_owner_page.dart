import 'package:flutter/material.dart';

class VerifiredOwnerPage extends StatelessWidget {
  const VerifiredOwnerPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
